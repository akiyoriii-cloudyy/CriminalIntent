<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_window_investigation">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:elevation="0dp"
        android:paddingStart="16dp"
        android:paddingTop="14dp"
        android:paddingEnd="16dp"
        android:paddingBottom="10dp">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/pager_toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_toolbar_casefile"
            android:minHeight="68dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            app:navigationIconTint="@color/crime_ink_dark"
            app:subtitleTextColor="@color/crime_ink_dark"
            app:titleTextColor="@color/crime_ink_dark" />
    </com.google.android.material.appbar.AppBarLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingStart="16dp"
            android:paddingTop="6dp"
            android:paddingEnd="16dp"
            android:paddingBottom="6dp">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/jump_to_first"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:backgroundTint="@color/crime_surface_soft"
                android:minHeight="52dp"
                android:text="@string/crime_jump_first"
                android:textColor="@color/crime_text_primary"
                app:cornerRadius="20dp"
                app:rippleColor="@color/crime_overlay"
                app:strokeColor="@color/crime_stroke"
                app:strokeWidth="1dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/jump_to_last"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:layout_weight="1"
                android:backgroundTint="@color/crime_surface_soft"
                android:minHeight="52dp"
                android:text="@string/crime_jump_last"
                android:textColor="@color/crime_text_primary"
                app:cornerRadius="20dp"
                app:rippleColor="@color/crime_overlay"
                app:strokeColor="@color/crime_accent_amber"
                app:strokeWidth="1dp" />
        </LinearLayout>

        <androidx.viewpager.widget.ViewPager
            android:id="@+id/crime_view_pager"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@android:color/transparent" />
    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
